plugins {
    id "org.springframework.boot" version "2.1.4.RELEASE"
}

apply plugin: 'java'
apply plugin: 'io.spring.dependency-management' //follows version set in plugins closure

repositories {
    mavenCentral()
}

dependencies {
    compile("org.springframework.boot:spring-boot-starter-web")
}